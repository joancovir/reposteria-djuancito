{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash ./start.sh" // <-- Ordena construir con el script completo
  },
  "deploy": {
    "startCommand": "exec java -jar backend/target/*.jar --server.port=$PORT" // <-- Ordena iniciar el JAR compilado
  }
}
