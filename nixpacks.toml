# nixpacks.toml  ← NOMBRE EXACTO, EN LA RAÍZ
providers = ["nodejs"]

[phases.setup]
nixPkgs = ["nodejs-20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build -- --configuration production"]

[start]
cmd = "npx serve -s dist/angular-temp -l $PORT"
