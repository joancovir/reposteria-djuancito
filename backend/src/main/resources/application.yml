cloudinary:
  cloud-name: ddk8osgyk
  api-key: 848718324627338
  api-secret: IlW5Xc70JZOIqM9gOdNo13JkpWM
  secure: true

spring:
  datasource:
    url: jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQLUSER}
    password: ${MYSQLPASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    static-path-pattern: /**
  web:
    resources:
      static-locations: classpath:/static/, file:frontend/

jwt:
  secret: ${JWT_SECRET}

# ← ESTAS 4 LÍNEAS SON LAS QUE HACEN QUE RAILWAY FUNCIONE
server:
  port: ${PORT:8080}
  address: 0.0.0.0
  servlet:
    context-path: /
